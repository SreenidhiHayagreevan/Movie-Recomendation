version: 1
backend:
  phases:
    build:
      commands:
        - npm install
    postBuild:
      commands:
        - echo "Backend deployment completed"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/* 